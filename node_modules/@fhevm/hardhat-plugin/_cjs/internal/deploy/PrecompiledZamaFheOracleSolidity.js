"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getZamaFheOracleSolidityArtifact = getZamaFheOracleSolidityArtifact;
exports.parseSepoliaZamaOracleAddress = parseSepoliaZamaOracleAddress;
const debug_1 = __importDefault(require("debug"));
const ethers_1 = require("ethers");
const fs = __importStar(require("fs"));
const picocolors = __importStar(require("picocolors"));
const error_1 = require("../../error");
const debug = (0, debug_1.default)("@fhevm/hardhat:addresses");
async function getZamaFheOracleSolidityArtifact(paths, contractName) {
    const modulePath = paths.resolveZamaFheOracleSolidityArtifactPath(contractName);
    const artifact = await Promise.resolve(`${modulePath}`).then(s => __importStar(require(s)));
    return { artifact, path: modulePath };
}
function parseSepoliaZamaOracleAddress(paths) {
    const p = paths.zamaFheOracleSolidityAddressSol;
    if (!fs.existsSync(p)) {
        throw new error_1.HardhatFhevmError(`Unable to locate ${p}, please make sure that the @zama-fhe package is properly installed.`);
    }
    const content = fs.readFileSync(p, "utf-8");
    const prefix = "address constant SepoliaZamaOracleAddress = ";
    const pos = content.indexOf(prefix);
    if (pos < 0) {
        throw new error_1.HardhatFhevmError(`Unable to parse SepoliaZamaOracleAddress at ${p}`);
    }
    const addr = content.substring(pos + prefix.length, pos + prefix.length + 42);
    debug(`Resolve ${picocolors.magentaBright("SepoliaZamaOracleAddress")}=${addr}, using solidity file at ${p}.`);
    return {
        SepoliaZamaOracleAddress: ethers_1.ethers.getAddress(addr),
    };
}
//# sourceMappingURL=PrecompiledZamaFheOracleSolidity.js.map