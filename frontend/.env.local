# Contract Address Configuration
# For local development, deploy the contract first:
# 1. Start Hardhat node: npx hardhat node
# 2. Deploy contract: npm run deploy:local
# 3. Copy the deployed address and replace the value below

# Sepolia Testnet (already deployed)
VITE_CONTRACT_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512

# For local network (localhost:8545), use:
# VITE_CONTRACT_ADDRESS=0x... (replace with your local deployment address)
